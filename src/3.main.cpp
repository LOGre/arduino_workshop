// Ajout de bibliothèques, prévient le compilateur que l'on va utiliser
// des fonctions décrites ailleurs
#include <Arduino.h>
/*

const PinMap PinMap_DAC[] = {};

void setup() {
  // Initialisation, appelé une fois
  // Généralement pour déclarer les I/O
  pinMode(D13, OUTPUT);
}

void loop() {
  // Déclare et initialise des variables
  uint8_t coefficient = 5;
  unsigned int temps_base_ms = 25;
  // Allume la led
  digitalWrite(D13, HIGH);
  delay(temps_base_ms*coefficient/100);
  // Eteints la LED
  digitalWrite(D13, LOW);
  delay(temps_base_ms*(100-coefficient)/100);
}
*/